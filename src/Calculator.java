public interface Calculator {
    double add(double first, double last);
    double sub(double first, double last);
    double mul(double first, double last);
    double div(double first, double last);
}

